# $Id: cronentry,v 1.6 2004/08/09 11:37:50 kozlik Exp $
#
# Shell commmand examples for running serweb cron jobs. If you have installed
# php as command line executabele, you can use first lines. Otherwise you must 
# use second lines and make sure if you have properly setting access rights in 
# .htaccess
#
# Server monitoring - read server statistics every minute
#
# * * * * *		php -q /your/serweb/dir/html/admin/cron_job/read_ser_moni.php | cat >> read_ser_moni.log
# * * * * *		wget -q -O - http://www.your.domain.org/admin/cron_job/read_ser_moni.php | cat >> read_ser_moni.log
#
#
# Send daily missed calls
# It should be run after midnight because it sending missed calls of previous day
#
# 5 0 * * *		php -q /your/serweb/dir/html/admin/cron_job/send_daily_missed_calls.php | cat >> send_daily_missed_calls.log
# 5 0 * * *		wget -q -O - http://www.your.domain.org/admin/cron_job/send_daily_missed_calls.php | cat >> send_daily_missed_calls.log

